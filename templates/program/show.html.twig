{% extends 'base.html.twig' %}

{% block title %}Program{% endblock %}

{% block body %}
    <h1>Série</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ program.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ program.title }}</td>
            </tr>
            <tr>
                <th>Summary</th>
                <td>{{ program.summary }}</td>
            </tr>
            <tr>
                <th>Poster</th>
                <td>{{ program.poster }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('program_index') }}">retour à la liste</a>

    <a class="btn btn-primary" href="{{ path('program_edit', {'id': program.id}) }}">édition</a>

    {{ include('program/_delete_form.html.twig') }}
{% endblock %}
